## 🚀 FastAPI URL Shortener


Высокопроизводительный сервис для сокращения ссылок на **Python 3.11 + FastAPI**.  
Проект полностью контейнеризирован (Docker + Docker Compose) и готов к развёртыванию и масштабированию.

---

## 🛠 Стек технологий

- **Язык / фреймворк**: Python 3.11, FastAPI  
- **База данных**: PostgreSQL  
- **ORM**: SQLModel (SQLAlchemy + Pydantic)  
- **Кэш**: Redis (кэширование для быстрых редиректов)  
- **DevOps**: Docker, Docker Compose  
- **Тестирование**: Pytest  

---

## ✨ Функционал

- **Генерация коротких ссылок** (уникальные коды длиной 7 символов).  
- **Мгновенный редирект** через Redis с минимальными задержками.  
- **Статистика переходов** по каждой короткой ссылке.  
- **Защита от коллизий**: проверка уникальности кода в БД.  
- **Docker‑first**: запуск всего стека одной командой.  

---

## 🚀 Запуск проекта

Требуется установленный **Docker** и **Docker Compose**.

1. **Клонируйте репозиторий**:

   ```bash
   git clone https://github.com/kite-house/URL-Shortener.git
   cd fastapi-url-shortener
   ```
2. **Пропишите файл .env**:
   ```
    DB_USER = ""
    DB_PASS = ""
    DB_HOST = ""
    DB_PORT = ""
    DB_NAME = ""
    
    REDIS_HOST = ""
    REDIS_PORT = ""
   ```

2. **Запустите контейнеры**:

   ```bash
   docker compose up -d --build
   ```

3. **Доступ к приложению**:

   - API: `http://localhost:8000`  
   - Документация Swagger: `http://localhost:8000/docs`  

---

## 🔗 Как получить короткую ссылку

1. **Создайте короткую ссылку через POST `/cutback`**.

   В терминале (PowerShell / bash):

   ```bash
   curl -X POST "http://localhost:8000/cutback" \
     -H "Content-Type: application/json" \
     -d '{"target_url": "https://www.test.com"}'
   ```

   Пример ответа:

   ```json
   {
     "slug" : Ajxj1,
     "long_url" : "https://www.test.com"
   }
   ```

   Важное поле здесь — **`slug`**.  
   В этом примере короткая ссылка будет:

   ```text
   http://localhost:8000/Ajxj1
   ```

   То же самое можно сделать через Swagger:

   - открыть `http://localhost:8000/docs`;
   - найти `POST /cutback`;
   - нажать **Try it out**, ввести `{ "target_url": "https://www.test.com" }` и нажать **Execute**;
   - взять `slug` из ответа.

2. **Перейдите по короткой ссылке**

   В браузере просто открыть:

   ```text
   http://localhost:8000/<url>
   ```

   Например:

   ```text
   http://localhost:8000/Ajxj1
   ```

   Пользователь будет перенаправлен на исходный `long_url`, а счётчик кликов увеличится.

---

## 📊 Информация об ссылке 

Для просмотра информации по конкретной короткой ссылке есть эндпоинт:

- **`GET /info/{url}`**

Пример запроса:

```bash
curl http://localhost:8000/info/Ajxj1
```

Пример ответа:

```json
{
    "slug" : Ajxj1
    "long_url" : https://www.test.com,
    "count_clicks" : 1,
    "date_created" : 25.12.2025
}
```

Поле **`count_clicks`** — это **количество переходов по короткой ссылке**.

---

## 🧪 Тестирование

При запущенном стеке выполните:

docker compose exec app pytest
(Тесты пройдут внутри контейнера приложения).
